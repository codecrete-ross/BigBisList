# 0.14.0 Release Evidence

Prepared 2026-09-05. This minor release adds the interface and interaction pass,
including density settings and migration to independent workspace filters.
The public identity, Anniversary interface 20506, canonical recommendations,
numeric recommendation scoring, and generated Data.lua remain unchanged.

## Schedule Review

Reviewed official announcements on 2026-09-05 at approximately 17:25 UTC against
`data/canonical/phases.json`. Blizzard's [raid opening announcement](https://us.forums.blizzard.com/en/wow/t/new-raids-open-at-300-pm-pdt-on-august-27/2340643/1)
confirms August 27 at 3 p.m. PDT, or `2026-08-27T22:00:00Z`
(`1787868000`), matching the existing Tier 6 boundary. The current official
[news listing](https://worldofwarcraft.blizzard.com/en-gb/news?category=1)
also identifies Black Temple as live. The review found no schedule change or
new exact Zul'Aman/Sunwell launch timestamp to apply. No canonical files were
changed and no live Wowhead scraping was performed.

## Automated Verification

The dedicated `.lua/release-venv` Python 3.12 environment was installed from this
repository's `pyproject.toml`, with beautifulsoup4 4.15.0 and Pillow 12.3.0.
Lua runtime and compiler are 5.1. The authoritative final gate is:

```powershell
pwsh -File scripts/check-release.ps1 -Version 0.14.0 -PythonPath .\.lua\release-venv\Scripts\python.exe -FullData
```

The full 437-test suite passed before the final request to expand Details
sections by default. Following that adjustment, all nine focused Details tests
and Lua compilation passed, including new coverage of default expansion,
explicit collapse, pooled rebinding, and phase matrix toggling. The full gate
must pass from the clean release commit before tagging; its result is recorded
in the handoff and hosted release workflow.

Additional coverage exercises reentrant scrolling during frame recycling,
workspace transitions, stale section actions, filter migration and clearing,
character restoration, modifier clicks, Undo, item/spell rebinding, and tooltip
expansion. Geometry checks cover 1020x560 and 1160x660 at both densities,
inspector and filter combinations, and screen-clamped widths. Performance
regressions assert bounded row realization and no data query rebuild for
presentation-only changes. Icon generation consistency is checked separately.

## Anniversary Client Evidence

Tester and release owner: the repository maintainer in this session.
Date: 2026-09-05. Installed executable metadata: `2.5.6.69546`.
The interface pass was deployed through `scripts/deploy.ps1 -NoClean` to
`G:\Battle.net Games\World of Warcraft\_anniversary_\Interface\AddOns\BigBiSList`;
all 15 runtime payload files matched the workspace by SHA-256. Saved variables
were not modified by deployment.

After reporting and checking the UI adjustments, the maintainer stated:
"looking pretty good. you can release this one". The subsequent request was to
keep Details sections collapsible but expanded by default; it is included in
this release and covered by the focused tests above.

This is a successful general client check and explicit release authorization.
Individual `/bbltest`, `/bbl status`, `/bbltest perf`, migration, and UI-scale
checks were not separately attested. The final default-expansion adjustment is
automatically verified but has no separately reported in-game check. The agent
could not perform native client interaction because no game window was
available. These checks are not claimed as completed. The maintainer's release
authorization and final directed adjustment govern proceeding with these
limitations; the maintainer owns follow-up verification during normal play.
Residual risk is a client-specific rendering, saved-state, or performance issue
not represented by the simulated UI checks.
